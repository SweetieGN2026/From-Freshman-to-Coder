#include <stdio.h>

int main() {
    int n, i, res = 0;
    printf("请输入人数n：");
    scanf("%d", &n);

    for (i = 2; i <= n; i++) {
        res = (res + 3) % i;
    }

    printf("最后留下的是原来第%d号的那位\n", res + 1);
    return 0;
}
//递归 版本 约瑟夫环问题
